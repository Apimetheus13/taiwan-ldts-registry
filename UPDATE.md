# 更新作業手冊

## 何時該更新

| 觸發條件 | 說明 |
|---|---|
| 定期 | 每季一次，比對官網名單家數與排序 |
| 名單異動 | 官網出現新增或移除的實驗室 |
| **附件文號改變** | 最重要的觸發點。文號一變即代表認證範圍可能異動，需重新解析該家 PDF |
| 效期追蹤 | 目前追蹤中：LDT0001 行動基因，附件效期至 2026-07-31 已屆滿仍列名，文號仍為 1150017903 |

官網的「發布日期」不會隨附件改版更新，**唯一能鎖定版本的鍵值是附件首頁的「FDA 品字第 ____ 號」**。
比對時請以文號為準，不要以發布日期判斷。

---

## 更新步驟

### 步驟 1　比對名單

開啟 https://www.fda.gov.tw/TC/siteList.aspx?sid=12204 （共 4 頁），
與 `data/` 內最新 Excel 的「實驗室總表」逐筆比對：

- 家數是否改變
- 是否有新增或移除的實驗室
- 各家的附件連結是否更換（連結中的 `id=` 參數會變）

若完全相同，本輪不需更新，僅需在 CHANGELOG 記錄「已比對，無異動」。

### 步驟 2　下載附件 PDF

點各家的「附件」連結下載 PDF，命名為 `LDT00XX_實驗室名稱.pdf`，
放進同一個資料夾。**檔名格式要固定**，解析腳本會用到編號。

只需下載有異動的那幾家，其餘沿用上次的檔案。

> 注意：食藥署的 `GetFile.ashx` 連結曾出現伺服端快取問題，
> 用程式批次抓取時可能重複回傳同一份檔案。手動下載或用瀏覽器下載較可靠。
> 下載後務必打開確認每份的認證編號與檔名相符。

### 步驟 3　執行解析

```bash
python3 scripts/parse.py          # → parsed_all.json，並印出每家的項目數與儀器區塊數
```

檢查輸出：某家的「instr」數若小於「items」數，代表有附表的儀器區塊沒抓到。
處理方式見下方「附表逐字錯位的處理」。

### 步驟 4　產生 Excel 與網頁

```bash
python3 scripts/build_v4.py                                 # → Excel
python3 scripts/build_html.py --public --out index.html     # 公開版：預設隱藏第二層推論
python3 scripts/build_html.py --out internal.html           # 內部版：預設顯示第二層
```

兩版是同一份資料、同一支腳本，差別只在初始顯示狀態，讀者按一下按鈕即可切換。
**務必由同一次建置產出，不要手動改動任一版的內容**，否則會出現版本不一致。
`internal.html` 已列入 `.gitignore`，不會被上傳。

### 步驟 5　驗證

`build_v4.py` 會印出 `items / cross-validated pass / serials`。
確認：

- items 數與名單相符
- cross-validated pass 應等於 items（含人工補齊者）
- 網頁開啟後，搜尋框右側應顯示「顯示 N / N 項」。**若該計數空白，代表 JavaScript 出錯**，
  請開瀏覽器主控台查看錯誤訊息

### 步驟 6　存檔與發布

1. 把新的 `index.html` 複製一份到 `archive/YYYY-MM-DD.html`
2. 新的 Excel 放進 `data/`
3. 更新 `CHANGELOG.md`
4. 更新 `README.md` 開頭的資料基準日與項目數
5. 上傳（commit / push），GitHub Pages 會自動重新發布

---

## 附表逐字錯位的處理

部分認證附件的附表頁採逐字排版，欄位文字互相穿插，自動解析會抓不到或抓到被污染的序號。

判斷方式：`build_v4.py` 產生的 Excel，「序號交叉驗證」欄會標示
「無儀器資料」或「部分序號待人工確認：⋯」。

處理流程：

1. 用 **pypdf** 重讀該份 PDF（兩套引擎的文字流順序不同，常能互相補位）：

```python
from pypdf import PdfReader
import unicodedata
t = unicodedata.normalize('NFKC',
    "\n".join(p.extract_text() or "" for p in PdfReader('LDT00XX_xxx.pdf').pages))
i = t.find('附表7.')
print(t[i:i+800])
```

2. 讀出正確的關鍵儀器設備字串
3. 寫進 `scripts/corrections.json`，鍵為 `"實驗室編號|附表號"`
4. 重新執行步驟 4

補齊的列會自動標示為「人工核對補齊」，不會與自動驗證通過的列混為一談。

> **不要用推測填補。** 若兩套引擎都讀不出來，就讓該欄留白並標示原因。
> 這份整理的核心價值在於分清楚哪些是原文、哪些是推論，
> 在第一層資料裡放猜測值會讓整個分層失去意義。

---

## 常見注意事項

**CJK 相容字**：部分 PDF 的「年」使用相容字 U+F98E 而非 U+5E74，外觀完全相同但字碼不同，
會導致日期正則比對失敗。`parse.py` 已對全文做 NFKC 正規化，勿移除。

**官方文件本身的誤植**：已知 4 處（見網頁稽核紀錄）。發現新的請記錄在稽核分頁，
保留原文、不要逕行修正。

**平台推論規則**：新機型出現時，在 `build_v4.py` 的 `SEQ_RULES` 增列序號樣式。
規則是依廠牌序號慣例整理，未經原廠文件驗證，新增時請一併標註信心等級。
